# NaoFu WT Hub 3.0.1

本工具用于在本地制作并替换《战争雷霆》相关字体与 **UI 图标包** 等资源，仅供学习与个人娱乐使用。

**3.0 代** 在 2.x 字体能力基础上，将 **「自定义 UI」**（战争雷霆图标包一键部署、备份与还原）作为主线能力

---

## 分发与构建说明（当前情况）

- **对外发布形态**：以 **Release 安装包**（例如 `NaoFu WT Hub Install.exe`）或 `**release` 目录完整压缩包** 为准。
- **未附带 Debug 版**：**当前发布渠道不提供** Debug 配置的可执行包或「开发版」压缩包。最终用户请只使用上述 Release 安装包或 `release` 整包。  
若你**从源码开发/调试**，只能玩玩 2.1.3 的内容
- **主程序升级方式**：Hub **不提供**应用内静默整包自动更新；换新版本请从 **GitHub Releases**、官网下载页等渠道**手动下载**新安装包或新压缩包。  
**「自定义 UI」页**仍可对**图标包素材**做版本检查与在线下载/部署（与游戏 `ui\` 相关），二者请勿混淆。

---

## 运行环境

- **系统**：Windows 10 或 Windows 11（64 位）
- **使用方式**：解压后即可使用，无需安装。双击 **NaoFu WT Hub.exe** 启动
- **依赖**：发布包为自包含，无需单独安装 .NET；界面依赖系统自带的 WebView2 运行时（Windows 10/11 通常已内置，若提示缺失请另行安装）

## 使用方法

1. **启动**：双击运行 **NaoFu WT Hub.exe**（或运行 bin 目录内的 **NaoFu WT Hub 3.0.1.exe**）
2. **自定义字体**：在界面中选择或拖入字体文件（支持 .ttf / .otf），选择替换目标后点击「开始制作」，制作完成可直接「导入到游戏 UI 文件夹」
3. **自定义 UI 图标包**：在「自定义 UI」页面查看图标包版本，一键下载并部署至游戏目录下 *ui（atlases / images / tex 等）；支持分项勾选；部署前自动 **.bak** 备份，可随时*恢复默认图标*

## 目录说明（发布包）

- **bin**：主程序与运行所需文件，请勿删除
- **tools**：字体处理相关程序（含子集工具等），请勿删除
- **ui / assets / font**：界面与字体资源（font 下含 source、builtin、custom），请勿删除
- **build**：制作完成后的输出会出现在此目录下
- **font/custom**：可将自定义字体放入此处（或通过界面选择）

## 声明与版权

- 本工具与《战争雷霆》及其开发商无隶属关系，请勿用于任何商业或未授权用途
- 字体与 UI 资源替换仅供个人学习与娱乐；使用本工具产生的一切后果由使用者自行承担
- 软件按「现状」提供，不提供任何明示或暗示的保证
- **隐私与数据**：本工具不收集、不上传任何个人数据；程序内用于区分「自己制作的」字体包等标识仅保存在本地
- **杀毒软件**：本工具为本地小工具，部分杀毒软件可能误报。请仅从官方或可信渠道下载

## 更新记录

### 3.0.1（2026-05-05）

- **3.0 代主版本**：新增「**自定义 UI**」——战争雷霆 **图标包**一键下载与部署（可勾选子项），部署至游戏 **ui*，支持 **.bak** 备份与一键还原
- **图标包**：支持版本检查与从分发服务器拉取**素材包**（与主程序安装包更新无关）
- **主程序分发**：已移除应用内**整包**静默更新及清单式自更新；升级 Hub 请从 Releases / 官网**手动**获取新包
- 下载中断时自动清理未完成的临时文件
- Launcher 使用 .NET 10 自包含发布（Release 包），无需用户单独安装运行时
- 主界面新增「自定义 UI」入口，菜单结构更新
- **根目录启动器**：修复部分环境下无法正确拉起带空格路径的 `bin` 内主程序的问题
- **发布脚本**：发布前自动确保 **nf_subset_tool.exe** 存在并复制到 **tools**；修正 PowerShell 下部分路径解析问题

### README 文档（2026-05-06）

- 补充**当前分发与构建说明**：对外仅 Release/安装包；**本次及当前渠道未附带 Debug 构建**；区分「图标包在线更新」与「主程序升级」

### 2.1.3 及更早

- 2.0代为第一代可视化 UI 界面

---

## 致谢与第三方

- 字体子集化使用 [fonttools](https://github.com/fonttools/fonttools)（MIT 许可证），特此致谢

---

以下为早期 1.10 简介（供参考）

# NaoFu's NaoFu WT Customize Font 1.10

## 战争雷霆自定义字体替换工具

### 项目简介

《战争雷霆》玩家此前替换游戏内字体比较麻烦。本工具通过二进制定位与替换，在保持官方 `fonts.vromfs.bin` 结构的前提下，把用户指定字体写进对应位置，替换后即可在游戏内生效（HUD、聊天等）。

### 使用方法（1.10 时期）

1. 准备你的字体文件，复制到 `custom_font`，重命名为 `MyFonts.ttf`
2. 双击运行 `NaoFu_WT_Font_Patcher.exe`，按序号选择要替换的字体
3. 完成后将 `build` 下的 `fonts.vromfs.MODDED.bin` 复制到游戏 `ui` 目录替换原文件
4. 启动游戏即可

### 版权与致谢（1.10）

- 本项目由 NaoFu (一只小脑斧) 独立制作，保留所有权利。B站UID: 405046590，技术交流Q群: 941587776。严禁将本工具及代码用于任何商业用途。
- 特别致谢：游戏文件解包使用了 **klensy** 的 **wt-tools**（[https://github.com/klensy/wt-tools）](https://github.com/klensy/wt-tools）)

