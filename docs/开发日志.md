# NaoFu WT Hub · 开发日志

维护：NaoFu

---

## 2026-03-29 23:20:53

- **版本**：统一采用语义化版本号 **2.1.3**（原 `2.11` 两段式写法不再沿用）。
- **字体子集工具**（`subset_tool.py`）：三参数模式增加**两轮瘦身**——首轮按参考字体字符集深度压缩；若仍大于参考文件大小，则第二轮用最小拉丁集（Basic Latin + Latin-1 Supplement，约 190 字）从用户字体裁剪，取两轮中更小者输出；可变字体在压平后去除 gvar/fvar 等。
- **补丁逻辑**（`nf_patcher.c`）：`auto_slim` 模式下槽位超出时跳过该槽位并继续，避免整批失败。
- **仓库**：`.gitignore` 补充 PyInstaller 缓存、`*.spec`、`*.bak`；清理本地编译产物与误追踪的 spec；提交并推送至 GitHub `main`。
- **发布**：本地执行 `build_release.ps1` 生成 `release/` 目录；在 GitHub 创建 **Release**（tag `v2.1.3`，标题 NaoFu WT Hub 2.1.3），上传压缩包。

---
